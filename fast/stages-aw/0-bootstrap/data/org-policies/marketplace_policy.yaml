---
# This boolean constraint, when enforced, disables public marketplace for all users under the org. By default, public marketplace access is enabled for the org.
# COULD HAVE
# commerceorggovernance.disablePublicMarketplace:
#   rules:
#   - enforce: false
